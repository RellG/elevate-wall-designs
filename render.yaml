services:
  - type: web
    name: elevate-wall-designs
    runtime: node
    plan: free
    region: ohio
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.11.1
    autoDeploy: true
